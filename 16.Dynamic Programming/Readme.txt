
Subset of n elements contains 2^n subsets

In a subsequence, the elements need to follow the order of the original array
whereas in a subset there is no constraint on the order of the elements. 
// in below one , it is coming like this
// INT_MAX + 1 = INT_MIN

A substring of a string is a subsequence in which all the characters are consecutive

A subarray of a array is a subsequence in which all the numbers are consecutive

// recursion 


###############################################################################################
// memoization


###############################################################################################
// tabular


###############################################################################################
// optimization